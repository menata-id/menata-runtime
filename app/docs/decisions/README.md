# ADR Index — `app/`'s own decision log

Numbered independently from `prototype/go/docs/decisions/`'s own `001`–`008` — see ADR-001's own
opening note. Read in number order for the history of `app/` itself; each ADR's own "Status"
line (and any appended "Status update" note, per root `CLAUDE.md`'s append-don't-rewrite
convention) is the current word on whether it still holds.

| ADR | Decision |
|---|---|
| [001](001-graduation-from-prototype.md) | Graduate `prototype/go`'s codebase as `app/`'s foundation, rather than starting from zero |
